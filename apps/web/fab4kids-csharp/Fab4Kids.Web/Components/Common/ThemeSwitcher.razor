@rendermode InteractiveServer
@inject Theming.ThemeState ThemeState
@inject IJSRuntime JsRuntime

<div class="theme-switcher" role="group" aria-label="Choose colour theme">
    @foreach (var option in Options)
    {
        <button
            type="button"
            class="theme-switcher__btn @(ThemeState.Current == option.Id ? "theme-switcher__btn--active" : string.Empty)"
            aria-label="@option.AriaLabel"
            aria-pressed="@(ThemeState.Current == option.Id ? "true" : "false")"
            @onclick="() => SelectAsync(option.Id)">
            <span aria-hidden="true">@option.Icon</span>
            <span class="theme-switcher__label">@option.Label</span>
        </button>
    }
</div>
