<!--
  Add this entry to the "Build and source link tooling" ItemGroup
  in Directory.Packages.props at the repo root.

  Nerdbank.GitVersioning is a build-time tool — PrivateAssets="all" is set
  in each .csproj that uses it, so it never appears as a package dependency
  in published .nupkg files. The version entry here just centralises the
  version number as with all other packages.

  Find the existing ItemGroup labelled:
      <ItemGroup Label="Build and source link tooling">

  And add:
-->

<PackageVersion Include="Nerdbank.GitVersioning" Version="3.7.115" />

<!--
  Then initialise Nerdbank.GitVersioning for the repo with the nbgv CLI tool.
  Run these commands once from the repo root:

      dotnet tool install -g nbgv
      nbgv install

  This creates a root-level version.json if one doesn't already exist.
  Each package that needs independent versioning gets its OWN version.json
  in its project folder — which overrides the root for that subtree.

  For AStar.Dev.Utilities, the version.json lives at:
      packages/core/AStar.Dev.Utilities/version.json

  The Tests.Unit project inherits the same version automatically because
  it sits in a subfolder of the package — no separate version.json needed.

  After adding version.json and committing, verify versioning works:
      nbgv get-version packages/core/AStar.Dev.Utilities
-->
